import { Routes } from '@angular/router';
import { RecetaViewComponent } from './features/receta-view/receta-view.component';

export const routes: Routes = [
    {path: 'principal', component: RecetaViewComponent},
];
