import { DuracionSinSegundosPipe } from './duracion-sin-segundos.pipe';

describe('DuracionSinSegundosPipe', () => {
  it('create an instance', () => {
    const pipe = new DuracionSinSegundosPipe();
    expect(pipe).toBeTruthy();
  });
});
